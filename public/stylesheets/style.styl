box_width = 150px;
box_height = 150px;
box_margin = 2px;
box_count = 4;

container_width = (box_width + box_margin * 2) * box_count;
container_height = (box_height + box_margin * 2) * box_count;

wrap_width = container_width;

numInput_width = 50px;

.box{
	width: box_width;
	height: box_height;
	margin: box_margin;

	float: left;
	background-color:black;
}

.container{
	width: container_width;
	height: container_height;
	margin: auto;
}

#wrap{
	width: wrap_width;
	margin: 0 auto;
}

.numInput{
	width: numInput_width;
}

h1{
	margin: 10px 0;
}

